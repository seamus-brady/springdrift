Session date: {{last_session_date}}
Status: {{session_status}}

Active threads: {{active_thread_count}} [OMIT IF ZERO]
{{active_threads}} [OMIT IF EMPTY]

Persistent facts: {{persistent_fact_count}} [OMIT IF ZERO]
{{recent_fact_sample}} [OMIT IF EMPTY]

CBR cases: {{cbr_case_count}} [OMIT IF ZERO]

Today's cycles: {{today_cycles}} [OMIT IF ZERO]
Success rate: {{today_success_rate}} [OMIT IF EMPTY]
Agent health: {{agent_health}} [OMIT IF EMPTY]

Profile: {{active_profile}} [OMIT IF EMPTY]
Agents: {{profile_agents}} [OMIT IF EMPTY]

Open commitments: {{open_commitments}} [OMIT IF EMPTY]
Memory health: {{memory_health}} [OMIT IF EMPTY]

At session start or after a gap in conversation, review recent memory context before responding. Use recall_recent if the injected memory summary above seems stale or insufficient.
